const checkFieldExist = require('./checkFieldExist');
const checkFieldValid = require('./checkFieldValid');
const checkProductExist = require('./checkProductExist');

module.exports = {
  checkFieldExist,
  checkFieldValid,
  checkProductExist,
};
